version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: $USERNAME/reverseproxy:latest
  backend_user:
    build:
      context: ./udagram-api-user
    image: $USERNAME/udagram-api-user:latest
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: $USERNAME/udagram-api-feed:latest
  frontend:
    build:
      context: ./udagram-frontend
    image: $USERNAME/udagram-frontend:latest
